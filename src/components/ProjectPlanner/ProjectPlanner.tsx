import styles from './ProjectPlanner.module.scss';
import React from 'react';

export const ProjectPlanner: React.FC = () => {
  return <div>
        <h2 className={styles.title}>Project Planner</h2>
    </div>;
};